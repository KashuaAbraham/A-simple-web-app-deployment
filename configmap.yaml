apiVersion: v1
kind: ConfigMap
metadata:
  name: my-app-config
  namespace: my-app-namespace
data:
  # Configuration values
  APP_NAME: "A Simple Web App"
  APP_ENV: "production"
  DATABASE_URL: "postgres://user:password@hostname:5432/dbname"
  REDIS_URL: "redis://hostname:6379"